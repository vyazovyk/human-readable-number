module.exports = function toReadable (number) {
  let massFirst = ["zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"];
  if (number.lenght = 1) {
    let humanFirst = number.toString().replace(number, massFirst[number]);
    return humanFirst;
  }
}
